package cs3500.freecell.model;

/**
 * Type for the 4 different suits a card can be in a valid deck
 * clubs (♣), diamonds (♦), hearts (♥), and spades (♠).
 */
public enum Suit {
  CLUBS, DIAMONDS, HEARTS, SPADES
}
